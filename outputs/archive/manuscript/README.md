# outputs/archive/manuscript

Authoritative results reported in the manuscript (Tables 4 and 5, Sections 4.3 and 4.4) — frozen snapshot of the main experimental configuration.

Source cell: `outputs/_ablation_archive/class_general/` (run_id `run_20260517_1716`, seed 42, BRFSS 2021 full cohort, main experimental configuration). The specific manuscript revision this snapshot corresponds to is recorded in `manifest.json` and `config.json` (library versions, hardware, git commit when applicable).

Do not overwrite. Working/scratch runs from `python run_main.py` should write to `outputs/scratch/` (configure via `configs/default.yaml: paths.output_dir`).
